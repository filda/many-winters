{
  "stryker-config": {
    "mutate": ["**/Maps/*.cs", "**/Population/*.cs", "**/Persistence/*.cs"],
    "reporters": ["html", "progress"],
    "concurrency": 4,
    "additional-timeout": 60000,
    "thresholds": {
      "high": 100,
      "low": 100,
      "break": 95
    }
  }
}
