{
  "stryker-config": {
    "mutate": ["**/*.cs", "!**/Continuity/*.cs", "!**/Program.cs"],
    "reporters": ["html", "progress"],
    "concurrency": 2,
    "additional-timeout": 60000,
    "thresholds": {
      "high": 100,
      "low": 100,
      "break": 95
    }
  }
}
