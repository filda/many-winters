<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Deliberately not in ManyWinters.sln, same reasoning as build/ManyWinters.Build.csproj:
    these are slow, Windows-only, need a real window and launch the full game, so they must
    not run as part of `dotnet test ManyWinters.sln` / the fast CI gate. The E2E Cake target
    runs `dotnet test` against this project by path instead.
  -->

  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="4.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ManyWinters.Tools\E2EHarness\ManyWinters.Tools.E2EHarness.csproj" />
  </ItemGroup>

  <!--
    The six E2E classes each launch their own godot under the same window title, so they must run
    one at a time: in parallel, FindWindowByTitle returns whichever instance it sees first and each
    test would drive and kill a random other test's game. xunit runs test classes in parallel by
    default, so pin them serial here.
  -->
  <ItemGroup>
    <None Update="xunit.runner.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
